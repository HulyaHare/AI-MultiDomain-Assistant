I want you to act as a senior AI engineer, backend architect, ML engineer, NLP engineer, and product-focused software architect at the same time.
Your task is to help design and generate a complete, production-style project from scratch, based on my previous two projects and my new target product idea.
I previously built two important systems, and I want the new project to intelligently reuse and extend the logic of both.

DO NOT explain the architecture in text. I only need the complete, production-ready, runnable code. Use inline comments to explain your logic.

MY PREVIOUS PROJECT 1: HEALTHCARE / SYMPTOM-BASED DISEASE PREDICTION AND RECOMMENDATION SYSTEM
In my previous healthcare project, I built a Flask-based machine learning web application. The system loaded multiple CSV datasets such as symptoms, precautions, workouts, descriptions, medications, and diets. A trained SVM model was loaded using pickle. The user entered symptoms manually. These symptoms were mapped into a predefined symptom dictionary, converted into a one-hot encoded input vector, and passed into the trained model. The model predicted a disease. Then the system used helper/enrichment logic to fetch related details.
Input → preprocessing → vectorization → classification → label mapping → enrichment → recommendation pipeline.

MY PREVIOUS PROJECT 2: MOVIE RECOMMENDATION SYSTEM
In my previous movie recommendation project, I used movie metadata and credits datasets. I loaded CSV files, merged them by title, selected only relevant columns, cleaned missing values, and parsed JSON-like string fields. The project focused on content-based recommendation.
Data ingestion → cleaning → feature extraction → representation building → similarity/ranking → recommendation pipeline.

NEW TARGET PROJECT
Mobile AI-Powered Multi-Domain Recommendation Assistant
This system must be a smart assistant that supports multiple domains in one unified product. It should feel like an intelligent assistant that can understand what the user wants, route the request to the correct specialized engine, run local ML logic, and respond in a clean, fast, and user-friendly way.

CORE PRODUCT VISION
The product should support at least these 3 domains:
1. Movie recommendation
2. Healthcare recommendation / symptom-based support
3. E-commerce / product recommendation

It should also support:
4. Natural language understanding / intent detection via LLM
5. Voice input and optionally voice output
6. Modular backend architecture
7. Mobile-friendly integration

IMPORTANT DESIGN PHILOSOPHY
I do NOT want a toy project. I want this to be designed as a serious portfolio-grade, CV-worthy, production-style AI system. The final output should be modular, scalable, professional, and technically impressive. I want you to generate the complete project implementation so I can run it immediately.

==================================================
PART 1 — REUSING OLD PROJECTS
==================================================
* The healthcare engine should reuse the classification-based ML pattern from my previous healthcare project.
* The movie engine should reuse the content understanding / metadata-based recommendation logic from my previous movie project.
* The new assistant should sit above these domain engines as an orchestration layer powered by an LLM.

==================================================
PART 2 — MODULES
==================================================
I want the system designed with a modular architecture. Break it into the following major layers and implement them:
1. Client / Frontend Layer: text input, voice input button, result cards.
2. Input Processing Layer: accept raw user text, normalize text.
3. Query Understanding Layer: extract useful entities via LLM.
4. Router Layer: send the request to the correct domain engine.
5. Domain Engines: Healthcare, Movie, E-commerce.
6. Data Access Layer: CSV data loading and processing.
7. Response Builder Layer: convert raw engine outputs into consistent JSON structure.
8. Voice Output Layer: text-to-speech integration.

==================================================
PART 3 — PROJECT ARCHITECTURE DECISION
==================================================
Design the project using a backend-first architecture. Use this clean tech stack:
* Python
* FastAPI
* scikit-learn, pandas, numpy
* pydantic, joblib/pickle
* gTTS or any TTS library
* langchain / openai (for orchestration)

==================================================
PART 4 — HEALTHCARE ENGINE DESIGN
==================================================
The healthcare engine must be based on my old symptom-based system.
1. Free text symptoms mapped to internal symptom labels.
2. Feature engineering (one-hot encoding).
3. Model inference using SVM.
4. Enrichment layer (disease description, precautions, medications, diet, workouts).

==================================================
PART 5 — MOVIE ENGINE DESIGN
==================================================
Do NOT use external APIs for Movie. Use local CSV files with Pandas and Scikit-learn for similarity matching, just like the Healthcare engine.
1. Local content-based similarity matching.
2. Metadata parsing (genres, keywords, cast, crew, overview).
3. Ranking logic combining genre/keyword similarity.
4. Result formatting (title, overview, rating, year).

==================================================
PART 6 — E-COMMERCE ENGINE DESIGN
==================================================
Do NOT use external APIs for E-Commerce. Use local CSV files with Pandas and Scikit-learn for similarity matching, just like the Healthcare engine.
1. Types of queries (category, budget, feature).
2. Local product retrieval based on text similarity and keyword matching.
3. Ranking logic (relevance, price, rating).
4. Response formatting (product title, price, rating).

==================================================
PART 7 — VOICE ENABLED INTERACTION
==================================================
Implement speech-to-text input logic and text-to-speech output. Provide error handling for empty transcriptions.

==================================================
PART 8 — UNIFIED RESPONSE SCHEMA
==================================================
Design a consistent JSON response schema using Pydantic for health responses, movie responses, product responses, and error responses.

==================================================
PART 9 — LLM INTEGRATION (CRITICAL) & ORCHESTRATION
==================================================
We will talk with AI in English only so no need to translate other languages.
Add an LLM layer (via OpenAI API or Langchain) that does: intent detection, entity extraction, conversation understanding, response generation.
The LLM must never say "I don't understand" immediately.

Rewrite /assistant/query endpoint flow:
1. Receive user input.
2. Send to LLM → extract intent, entities. If no clear intent, use previous conversation context.
3. Route to correct engine (health / movie / ecommerce). All using local CSVs/Models.
4. Get structured result.
5. Send result back to LLM.
6. LLM generates natural response.

Implement session-based memory. Memory must store intent, extracted entities, last result.

==================================================
PART 10 — UI / FRONTEND (VERY IMPORTANT)
==================================================
Build a mobile-style chat interface using Vanilla HTML/JS/CSS (Tailwind) served directly via FastAPI templates. It MUST look exactly like a native mobile chat app, but require NO frontend build steps.
UI must include:
* Chat screen: message bubbles (user vs assistant).
* Input bar: text input, send button, microphone button.
* Dynamic responses:
  - If health: show disease, suggestions, warnings.
  - If movie: show movie cards (title, rating).
  - If ecommerce: show product cards (name, price, rating).
* Loading state: show "thinking..."
* Error handling: instead of failing, ask smart follow-up questions or say: "I interpreted this as a healthcare question..."

==================================================
PART 11 — DELIVERABLES I WANT FROM YOU
==================================================
1. Complete, runnable codebase in Python (FastAPI).
2. The HTML/JS/CSS frontend code to be served by FastAPI.
3. Clear folder structure.
4. Example dummy data generation snippets if CSVs are missing, so I can run it immediately.
Remember: DO NOT write long explanations. Just give me the code and file structures with inline comments. Make it robust and production-ready.